Options -Indexes

<Files "deploy.php">
    Order allow,deny
    Allow from all
</Files>

# C5 — PA8: redirecionar /principal/painel/ → /painel/ (301)
RewriteEngine On
RewriteRule ^painel(/.*)?$ /painel$1 [R=301,L]
